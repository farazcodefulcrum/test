{
  "routes": [
    {
      "method": "GET",
      "path": "/portfolio-cards",
      "handler": "portfolio-card.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/portfolio-cards/count",
      "handler": "portfolio-card.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/portfolio-cards/:id",
      "handler": "portfolio-card.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/portfolio-cards",
      "handler": "portfolio-card.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/portfolio-cards/:id",
      "handler": "portfolio-card.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/portfolio-cards/:id",
      "handler": "portfolio-card.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
