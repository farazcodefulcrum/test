{
  "routes": [
    {
      "method": "GET",
      "path": "/image-sliders",
      "handler": "image-slider.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/image-sliders/count",
      "handler": "image-slider.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/image-sliders/:id",
      "handler": "image-slider.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/image-sliders",
      "handler": "image-slider.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/image-sliders/:id",
      "handler": "image-slider.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/image-sliders/:id",
      "handler": "image-slider.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
